# Multimodal-DB Full Requirements
# Complete dependency list including all optional features

# Core Data Processing (Required)
polars>=1.0.0
pyarrow>=10.0.0

# Vector Database (Recommended)
qdrant-client>=1.7.0

# Ai agents and LLMs (Optional)
llama-index-experimental>=0.6.3

# API Backend (Required for API)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
websockets>=10.0

# User Interface (Required for UI)
gradio>=4.0.0
requests>=2.28.0

# Testing (Development)
pytest>=7.0.0
pytest-cov>=3.0.0
pytest-asyncio>=0.20.0

# Code Quality (Development)
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Utilities
python-dotenv>=1.1.1
pydantic>=2.0.0,<3.0.0
